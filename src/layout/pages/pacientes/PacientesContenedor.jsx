import * as React from "react";
import { Outlet } from "react-router-dom";

const PacientesContenedor = () => {
    return (
        <>
            <Outlet />
        </>
    );
};

export default PacientesContenedor;
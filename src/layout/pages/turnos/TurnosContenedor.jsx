import * as React from "react";
import { Outlet } from "react-router-dom";

const TurnosContenedor = () => {
    return (
        <>
            <Outlet />
        </>
    );
};

export default TurnosContenedor;
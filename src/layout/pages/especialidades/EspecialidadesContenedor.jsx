import * as React from "react";
import { Outlet } from "react-router-dom";

const EspecialidadesContenedor = () => {
    return (
        <>
            <Outlet />
        </>
    );
};

export default EspecialidadesContenedor;
